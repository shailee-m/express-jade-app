extend layout


block content 
    div.container
        form(name="selF",method="POST").row#selF.col-lg-9.col-sm-9.col-md-9.justify-content-center
            label(for="nam") Enter Name:
            input(type="text",name="nam",placeholder="Shailee",required=true).form-control.mt-15#ip1.required
            label(for="ch") Pick a choice:
            select(name="ch",required=true).form-control.mt-15#ip2
                //- var chs = {choice}
                option(value='',selected=true) none

                each item in choice
                    option(value=item,selected=false) #{item}
            button(type="submit",disabled=true)#sub.mt-15.btn.btn-primary send

        
            p.row#respText 



        //+selectForm(#{choice})
    
    //mixin selectForm(arr)
        